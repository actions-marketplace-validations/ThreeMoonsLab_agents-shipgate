version: "0.1"

project:
  name: simple-crewai-agent

agent:
  name: support-case-crew
  declared_purpose:
    - look up and summarize read-only support case metadata

environment:
  target: local

tool_sources:
  - id: crewai_agent
    type: crewai
    path: crew.py
